import 'package:flutter/material.dart';

class players_detail extends StatelessWidget {
  const players_detail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}