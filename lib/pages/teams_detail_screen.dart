import 'package:flutter/material.dart';

class teams_detail extends StatelessWidget {
  const teams_detail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
