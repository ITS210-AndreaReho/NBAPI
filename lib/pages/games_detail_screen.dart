import 'package:flutter/material.dart';

class games_detail extends StatelessWidget {
  const games_detail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
